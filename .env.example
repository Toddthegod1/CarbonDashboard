# =============================
# Carbon Tracker — .env.example
# =============================

# ----- App -----
APP_NAME=CarbonTracker
FLASK_ENV=production
# Generate a long random string:  python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME
PORT=8000
LOG_LEVEL=INFO
HEALTHCHECK_PATH=/health
PUBLIC_URL=http://<EC2-WEB-PUBLIC-DNS>   # or https://your-domain

# ----- Database (PostgreSQL on RDS) -----
DB_ENGINE=postgresql
DB_HOST=<your-rds-endpoint>.rds.amazonaws.com
DB_PORT=5432
DB_NAME=<your-db-name>
DB_USER=<your-db-user>
DB_PASSWORD=<your-db-password>
DB_SSLMODE=prefer    # or "require" if you enabled TLS on RDS

# ----- AWS + S3 (reports) -----
AWS_REGION=us-east-1
S3_BUCKET=carbon-tracker-reports-todd
S3_PREFIX=reports/
SIGNED_URL_TTL_SECONDS=3600   # if your UI generates time-limited download links

# ----- Worker (report generation) -----
# The systemd timer handles schedule; these can guide your app logic if needed.
REPORT_SCHEDULE=monthly
REPORT_OUTPUT_FORMATS=csv,pdf